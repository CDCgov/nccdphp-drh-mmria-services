{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "build",
      "command": "dotnet",
      "type": "process",
      "args": [
        "build",
        "${workspaceFolder}/mmria.services/mmria.services.csproj"
      ],
      "problemMatcher": "$msCompile"
    },
    {
      "label": "🔄 Pull Common Subtree - Services",
      "type": "shell",
      "command": "cd $(git rev-parse --show-toplevel) && git subtree pull --prefix=common https://github.com/CDCgov/nccdphp-drh-mmria-common.git main --squash",
      "problemMatcher": [],
      "presentation": {
        "reveal": "always",
        "panel": "new"
      }
    },
    {
      "label": "⬆️ Push Common Subtree - Services",
      "type": "shell",
      "command": "powershell.exe",
      "args": [
        "-NoProfile",
        "-Command",
        "Copy-Item -Path 'common/*' -Destination '../nccdphp-drh-mmria-common/' -Recurse -Force; Push-Location '../nccdphp-drh-mmria-common'; git add .; if (git status --porcelain) { git commit -m \"Sync from main repo $(Get-Date -Format 'yyyy-MM-dd HH:mm')\"; git push origin main; Write-Host '✅ Pushed!' -ForegroundColor Green } else { Write-Host 'No changes' -ForegroundColor Yellow }; Pop-Location"
      ],
      "options": {
        "cwd": "${workspaceFolder}"
      },
      "problemMatcher": [],
      "presentation": {
        "reveal": "always",
        "panel": "new"
      }
    }
  ]
}
